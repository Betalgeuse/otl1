# 원씽 잔디 표시 계약

## 0. Reference
사용자가 제시한 GitHub 기여 잔디의 작은 정사각형, 기본 회색·연두·초록 상태, 고정 4열과 Day·날짜 병기를 따른다. 마케팅 페이지나 GitHub 화면 복제는 범위가 아니다. Slack 기본 조작 요소와 동적으로 생성하는 PNG만 구현한다.

## 1. Identity
작은 기록이 날짜 순서대로 남는 단정한 보드. 보드의 위치는 고정하고 상태색만 변한다. 완료량으로 날짜를 당기거나 실패를 강조하지 않는다.

## 2. Color
- background #FFFFFF, text #24292F, secondary #57606A, border #D0D7DE.
- empty #EBEDF0, written #9BE9A8, complete #216E39.
- today #0969DA. 사용자 지정 상태색과 별개로 오늘 테두리를 표시.
- 세 상태색은 사용자별 #RRGGBB. 상태 기호는 배경에 대비되는 검정/흰색을 계산해서 사용.
- future는 empty 색에 점선 테두리. 상태 의미는 색만으로 전달하지 않는다.

## 3. Typography
Slack의 제목·본문·조작부는 네이티브 글꼴. PNG는 라이브러리·외부 폰트 없이 숫자와 DAY, /, 상태 기호를 그리는 고정 글리프 사용. 칸 표기와 날짜를 같은 좌표계에서 렌더링한다. 모바일 300px 표시 폭에서 Day·날짜가 약 12px 이상이 되도록 구성한다. 긴 목표와 한글 설명은 이미지 밖 Slack 텍스트에 둔다.

## 4. Layout
4열 고정. 첫 보드 1행, 이후 최대 2행. 보드 논리 폭 320px, 좌우 8px, 열 너비 76px. Day·날짜 라벨과 정사각형을 하나의 Cell primitive로 재사용. 2배 해상도 640px PNG. 고정 날짜, 누락 칸 유지, 가로 스크롤 없음.

## 5. Components
- Cell: Day 번호, M/D, 상태 사각형. 미작성(-), 작성(점), 완료(체크), 미래(빈 점선), 오늘(추가 테두리). 조작 대상 아님.
- Board: 같은 Cell을 4열에 날짜 순으로 배치. 제목·한국 시간 기간·상태 합계·목표 텍스트·범례는 Slack 블록으로 제공.
- Complete button: 날짜·작성자·공개 여부를 값으로 가지고, 누른 사람이 작성자와 일치할 때만 기록 변경. 다른 사람이 누르면 비공개 안내만 반환. 완료 후에는 완료 취소 버튼.
- Palette modal: 상태별 기본 제공 선택과 선택적 직접 HEX 입력. 잘못된 HEX는 필드 오류. 저장·기본색 복원. Slack 네이티브 focus·loading·keyboard 동작 사용.
- History: 명령 인자로 기준 날짜를 지정해서 과거 보드를 확인. 보드 이미지 칸 클릭은 제공하지 않는다.
- Quick menu: daily-scrum의 공통 안내와 개인 안내에서 사용설명서, 자기소개 작성, 자기소개 모음, 밀린 후기 기록하기, 친구 초대를 같은 순서로 제공한다. 채널 상단 북마크와 핀 메시지도 같은 진입점을 유지한다.
- Core entry controls: daily-scrum 안내에서 `ONE THING 기록하기`와 `후기 남기기`를 별도 조작 행의 첫 두 버튼으로 둔다. 목표는 primary, 후기는 danger 스타일로 구분하며, 두 버튼 모두 모달 입력 뒤 사용자를 멘션한 공개 글과 리액션·잔디가 같은 대화에 이어진다.
- Introduction canvas: 공개 자기소개를 한 Canvas에서 회원별 제목과 본문으로 나누고, 자기소개 수정 뒤 같은 문서를 갱신한다. 개별 자기소개 메시지와 Canvas는 동일한 현재 revision을 사용한다.
- Past review modal: 잔디 아래에는 가장 최근의 미정리 날짜를 최대 3개만 버튼으로 보여준다. 모달은 선택한 날짜·목표를 고정 표시하고 완료·일부 진행·미완료와 후기 한 줄을 한 번에 받는다.

## 6. Interaction
애니메이션 없음. 새 /one 작성·조회 응답은 작성자 표시와 함께 채널 전체에 공개. 완료·색상 변경은 원래 메시지의 공개 여부를 유지하며 갱신. 색상 설정·지난 후기 모달과 오류·초대 코드는 비공개. 기존 개인 메시지는 소급 공개하지 않는다. 링크 버튼은 Slack Canvas를 직접 열고 action 수신은 상태를 바꾸지 않는다.

## 7. Surface
PNG는 밝은 불투명 배경을 유지해 Slack 다크 모드에서도 글자 대비를 보장. 칸에는 얇은 경계와 작은 모서리 반경 사용. 그림자·장식 없음.

## 8. Accessibility and verification limits
상태별 기호, 텍스트 범례, 각 날짜 상태를 포함한 alt_text. 이미지 밖에 목표와 집계 제공. 모든 핵심 링크 버튼에는 의미가 분명한 한국어 label과 accessibility_label을 둔다. 네이티브 모바일 Slack은 실제 연결 전 미검증. 개발용 브라우저 가독성 검증을 Slack 실기기 검증으로 보고하지 않는다.
